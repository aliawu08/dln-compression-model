[project]
name = "dln-computational-framework"
version = "0.1.0"
description = "DLN computational framework simulations."
requires-python = ">=3.8"
dependencies = [
  "numpy",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
